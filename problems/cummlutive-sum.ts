export default function runningSum(nums: number[]) {
  // code here

  if (!Array.isArray(nums) || nums.some((n) => typeof n !== "number"))
    throw new Error("Error");

  let commutative_sum = 0;

  return [];
}
